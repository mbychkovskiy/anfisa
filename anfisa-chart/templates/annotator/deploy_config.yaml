apiVersion: apps.openshift.io/v1
kind: DeploymentConfig
metadata:
  name: anfisa-annotation-test
  labels:
    app: anfisa-annotation-test
    deploymentconfig: anfisa-annotation-test
spec:
  replicas: 1
  selector:
    app: anfisa-annotation-test
    deploymentconfig: anfisa-annotation-test
  template:
    metadata:
      labels:
        app: anfisa-annotation-test
        deploymentconfig: anfisa-annotation-test
    spec:
      containers:
        - name: anfisaannotation
          image: "image-registry.openshift-image-registry.svc:5000/test/anfisa-annotation-test"
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          tty: true
          stdin: true
          serviceAccount: default
      terminationGracePeriodSeconds: 5
  triggers:
  - type: ConfigChange
  - imageChangeParams:
      automatic: true
      containerNames:
      - anfisaannotation
      from:
        kind: ImageStreamTag
        name: image-registry.openshift-image-registry.svc:5000/test/anfisa-annotation-test:latest
    type: ImageChange
  strategy:
    type: Recreate
