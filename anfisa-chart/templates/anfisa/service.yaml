apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.anfisa.name }}
  labels:
    app: {{ .Values.anfisa.name }}
  
spec:
  ports:
  - name: {{ .Values.anfisa.name }}
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: {{ .Values.anfisa.name }}
  sessionAffinity: None
  type: ClusterIP