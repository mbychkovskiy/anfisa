apiVersion: v1
kind: Service
metadata:
  name: anfisa-test
  labels:
    app: anfisa-test
  
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: anfisa-test
  sessionAffinity: None
