apiVersion: v1
kind: Service
metadata:
  name: anfisa-v6
  labels:
    app: anfisa-v6
  
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: anfisa-v6
  sessionAffinity: None
