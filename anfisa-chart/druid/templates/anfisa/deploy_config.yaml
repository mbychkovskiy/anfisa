apiVersion: apps.openshift.io/v1
kind: DeploymentConfig
metadata:
  name: anfisa-v6
  labels:
    app: anfisa-v6
    deploymentconfig: anfisa-v6
spec:
  replicas: 1
  selector:
    app: anfisa-v6
    deploymentconfig: anfisa-v6
  template:
    metadata:
      labels:
        app: anfisa-v6
        deploymentconfig: anfisa-v6
    spec:
      containers:
        - name: anfisa6
          image: "image-registry.openshift-image-registry.svc:5000/asset-forome-dev/anfisa-v6"
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          tty: true
          stdin: true
          serviceAccount: default
      terminationGracePeriodSeconds: 5
  triggers:
  - type: ConfigChange
  - imageChangeParams:
      automatic: true
      containerNames:
      - anfisa6
      from:
        kind: ImageStreamTag
        name: image-registry.openshift-image-registry.svc:5000/asset-forome-dev/anfisa-v6:latest
    type: ImageChange
  strategy:
    type: Rolling
